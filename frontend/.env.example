# Copiá como `.env` y completá.
# Las variables de Vite que empiezan con VITE_ son accesibles desde el código frontend.

# URL del backend (sin slash final)
VITE_API_URL=http://localhost:4000/api

# Número de WhatsApp del dueño (con código de país, sin + ni espacios)
VITE_WHATSAPP_NUMBER=573184280375
